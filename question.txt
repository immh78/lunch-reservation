files이 아니라 "예약 내용입니다."라는 내용을 공유하려면?

if (navigator.share) {
                await navigator.share({
                    title: '포장예약메뉴 및 선결제 내역',
                    files: [new File([await (await fetch(image)).blob()], 'table.png', { type: 'image/png' })],
                });
            } else {
                alert('공유 API를 지원하지 않는 브라우저입니다.');
            }